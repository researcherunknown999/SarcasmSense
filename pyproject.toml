[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sarcasmsense"
version = "0.1.0"
description = "Reproducible SarcasmSense experiments (data + loaders)"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
